=== Filters

The following filter implementation exist:

include::jQAssistant:Rules[concepts="filter:ListFilter"]

This is the package content of the `filter` package:

include::jQAssistant:Rules[concepts="filter:FilterPackage"]
